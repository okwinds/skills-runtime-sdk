---
name: data_qa_reporter
description: "数据 QA 与报告（人类应用示例）：shell_exec 校验并输出 report.md。"
metadata:
  short-description: "QA+Report：shell_exec + file_write。"
---

# data_qa_reporter（app / QA+Report）

## 目标

- 使用 `shell_exec` 做最小确定性 QA（解析 fixed.csv 并断言结构）
- 使用 `file_write` 输出 `report.md`


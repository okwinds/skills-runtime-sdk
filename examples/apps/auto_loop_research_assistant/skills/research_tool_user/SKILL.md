---
name: research_tool_user
description: "研究助手（人类应用示例）：使用 grep_files/read_file 在本地知识库中检索与阅读。"
metadata:
  short-description: "Tools：grep_files + read_file。"
---

# research_tool_user（app / Tools）

## 目标

- 使用 `grep_files` 找到包含关键词的文件
- 使用 `read_file` 读取关键证据

## 必须使用的工具

- `grep_files`
- `read_file`


---
name: incident_triager
description: "排障助手（人类应用示例）：读取日志并提出澄清问题。"
metadata:
  short-description: "Triage：read_file + request_user_input。"
---

# incident_triager（app / Triage）

## 目标

- 使用 `read_file` 读取 `incident.log`
- 用 `request_user_input` 向用户提出 2~3 个关键澄清问题

## 必须使用的工具

- `read_file`
- `request_user_input`


# 示例：OpenAICompatible（不要提交真实密钥）
OPENAI_API_KEY=
# 可选：OpenAI-compatible base_url（到 /v1 为止）
OPENAI_BASE_URL=https://api.openai.com/v1

# 可选：模型名（需要支持 tool calling）
SRS_MODEL_PLANNER=gpt-4o-mini
SRS_MODEL_EXECUTOR=gpt-4o-mini

